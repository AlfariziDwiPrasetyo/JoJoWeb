<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <title>Document</title>
</head>
<body style="font-family: 'Poppins', sans-serif;">
    <!DOCTYPE html>
<html lang="en">
<head>
  <div class="container">
    <div class="row">
        <% datas.forEach(data => { %>
        <div class="col-md-4 col-12 mt-4 w-50">
            <div class="card">
              <img src="https://jojos-bizarre-api.netlify.app/assets/<%= data.image %>" class="pt-2 card-img-top m-auto" style="width: 200px; height: 200px; float: left; object-fit: contain;" alt="Image 1">
              <div class="card-body">
                <h5 class="card-title"><a class="text-decoration-none" href="/char/<%= data.id %>"><%= data.name %></a></h5>
                <p class="card-text"><%= data.chapter %></p>
              </div>
            </div>
        </div>
        <% }) %>
    </div>
  </div>
</body>
</html>